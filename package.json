{
  "name": "zaeinstream-web",
  "version": "1.0.0",
  "description": "ZAEINSTREAM - Streamex-style streaming web with Drive Index + Subsource subtitles",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets:subsource": "wrangler secret put SUBSOURCE_API_KEY",
    "secrets:supabase": "wrangler secret put SUPABASE_SERVICE_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
